% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration.R
\name{optimizeIntegrationParameters}
\alias{optimizeIntegrationParameters}
\title{Optimize Parameters for Multi-Modal Integration}
\usage{
optimizeIntegrationParameters(
  data_list,
  modality_names = NULL,
  cell_type_var = "cell_type",
  optimization_metric = "mixing",
  secondary_metrics = c("silhouette", "kbet", "lisi"),
  methods = c("canonical_correlation", "harmony", "mnn", "seurat"),
  n_dims_range = c(10, 20, 30),
  n_features_range = c(1000, 2000, 3000),
  max_combinations = 10,
  n_iterations = 1,
  reference_batch = NULL,
  return_all_results = FALSE,
  parallel = FALSE,
  n_cores = 2,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{data_list}{List of data objects (Seurat or SingleCellExperiment) for different modalities}

\item{modality_names}{Names of modalities corresponding to each item in data_list}

\item{cell_type_var}{Name of the variable containing cell type annotations}

\item{optimization_metric}{Primary metric to optimize: "mixing", "silhouette", "kbet", or "lisi" (default: "mixing")}

\item{secondary_metrics}{Additional metrics to compute (default: all available metrics)}

\item{methods}{Vector of integration methods to test (default: all available methods)}

\item{n_dims_range}{Range of dimensions to test (default: c(10, 20, 30))}

\item{n_features_range}{Range of features to test (default: c(1000, 2000, 3000))}

\item{max_combinations}{Maximum number of parameter combinations to test (default: 10)}

\item{n_iterations}{Number of iterations for more reliable metrics (default: 1)}

\item{reference_batch}{Optional reference batch/modality for comparison}

\item{return_all_results}{Logical, whether to return all results or just the best (default: FALSE)}

\item{parallel}{Logical, whether to use parallel processing (default: FALSE)}

\item{n_cores}{Number of cores to use for parallel processing (default: 2)}

\item{seed}{Random seed for reproducibility (default: 42)}

\item{verbose}{Logical, whether to show progress messages (default: TRUE)}
}
\value{
A list containing optimized parameters and integration results
}
\description{
Automatically tests different integration parameters and selects optimal settings
based on quality metrics, helping users achieve the best possible integration results.
}
