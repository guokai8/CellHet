% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment_richr.R
\name{runRichREnrichment}
\alias{runRichREnrichment}
\title{Functional Enrichment Analysis using richR}
\usage{
runRichREnrichment(
  deg_results,
  gene_list = NULL,
  analysis_type = "GO",
  organism = "human",
  ontology = "BP",
  keytype = "SYMBOL",
  pvalue = 0.05,
  qvalue = 0.05,
  minGSSize = 10,
  maxGSSize = 500,
  use_significant_only = TRUE,
  separate_direction = FALSE,
  build_annotation = TRUE,
  visualize = TRUE,
  plot_type = "dot",
  top_terms = 20,
  return_plots = TRUE
)
}
\arguments{
\item{deg_results}{Output from compareDEGs() or a vector of gene names}

\item{gene_list}{Optional named vector of genes (names are gene symbols, values are scores/logFC)}

\item{analysis_type}{Type of enrichment: "GO", "KEGG", "GSEA", or "DAVID" (default: "GO")}

\item{organism}{Organism name: "human", "mouse", "rat", etc. (default: "human")}

\item{ontology}{For GO analysis: "BP", "MF", "CC", or "ALL" (default: "BP")}

\item{keytype}{Gene ID type: "SYMBOL", "ENTREZID", "ENSEMBL" (default: "SYMBOL")}

\item{pvalue}{P-value cutoff for enrichment (default: 0.05)}

\item{qvalue}{Q-value cutoff for multiple testing (default: 0.05)}

\item{minGSSize}{Minimum gene set size (default: 10)}

\item{maxGSSize}{Maximum gene set size (default: 500)}

\item{use_significant_only}{Use only significant DEGs (default: TRUE)}

\item{separate_direction}{Separate up/down regulated genes (default: FALSE)}

\item{build_annotation}{Whether to build annotation if not available (default: TRUE)}

\item{visualize}{Create visualization plots (default: TRUE)}

\item{plot_type}{Type of plot: "bar", "dot", "network", "all" (default: "dot")}

\item{top_terms}{Number of top terms to display (default: 20)}

\item{return_plots}{Return plot objects (default: TRUE)}
}
\value{
A list containing:
\itemize{
\item enrichment_results: Enrichment analysis results
\item plots: List of ggplot objects (if visualize=TRUE)
\item summary: Summary statistics
\item gene_annotation: Annotation data used
}
}
\description{
Perform functional enrichment analysis on DEG results using the richR package.
Supports GO, KEGG, and custom gene set enrichment with visualization options.
}
\details{
This function wraps the richR package functionality to provide easy enrichment
analysis for CellHet DEG results. It supports:
\itemize{
\item GO enrichment (Biological Process, Molecular Function, Cellular Component)
\item KEGG pathway enrichment
\item Gene Set Enrichment Analysis (GSEA)
\item DAVID online analysis
}

The function can work with:
\enumerate{
\item DEG results from compareDEGs() - automatically extracts significant genes
\item Custom gene list - provide vector of gene symbols
\item Ranked gene list - provide named vector for GSEA
}
}
\examples{
\dontrun{
# Basic GO enrichment on DEG results
enrich_results <- runRichREnrichment(
  deg_results,
  analysis_type = "GO",
  organism = "human"
)

# KEGG pathway enrichment
kegg_results <- runRichREnrichment(
  deg_results,
  analysis_type = "KEGG",
  organism = "mouse"
)

# GSEA with ranked genes
ranked_genes <- setNames(deg_df$avg_log2FC, deg_df$gene)
gsea_results <- runRichREnrichment(
  ranked_genes,
  analysis_type = "GSEA",
  organism = "human"
)

# Separate up and down regulated genes
enrich_separate <- runRichREnrichment(
  deg_results,
  separate_direction = TRUE
)
}

}
